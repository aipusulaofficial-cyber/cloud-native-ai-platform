apiVersion: apps/v1
kind: Deployment
metadata: {name: cloud-native-ai-platform}
spec:
 replicas: 2
 selector: {matchLabels: {app: cloud-native-ai-platform}}
 template:
  metadata: {labels: {app: cloud-native-ai-platform}}
  spec:
   containers:
   - name: api
     image: cloud-native-ai-platform:latest
     ports: [{containerPort: 8000}]
     readinessProbe: {httpGet: {path: /health/ready, port: 8000}}
     livenessProbe: {httpGet: {path: /health/live, port: 8000}}
